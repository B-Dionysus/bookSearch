import React from "react";
// default context object with properties corresponding to Provider values

const SexyContext = React.createContext({
    sexySt:false,
    sexy:()=>{}
  });

export default SexyContext;
